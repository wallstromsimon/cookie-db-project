To complie the java program run from terminal:
	make
To run the compiled program run from terminal:
	make run

The program automatically connects to the database. 

The source files are in /src/cookie/
The class files will end up in /bin/, to clean the bin folder run from terminal:
	make clean

